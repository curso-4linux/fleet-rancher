---
namespace: gitops
targetCustomizations:

- name: test
  clusterSelector:
    matchLabels:
      env: test
  yaml:
    overlays:
    - scale3

- name: homolog
  clusterSelector:
    matchLabels:
      env: homolog
  yaml:
    overlays:
    - version
